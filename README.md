# JavaLabAssignment5

**A multithreaded, file-based Student Record Management System implemented using OOP (abstract classes, inheritance, interfaces), collections, sorting, and custom exception handling.**
